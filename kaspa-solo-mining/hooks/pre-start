#!/usr/bin/env bash
set -euo pipefail

APP_SRC_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"

install -d -m 0777 -o 1000 -g 1000 "${APP_DATA_DIR}/data"
install -d -m 0777 -o 1000 -g 1000 "${APP_DATA_DIR}/kaspad_data"

# Ensure runtime config.yaml exists for Stratum Bridge
if [ ! -f "${APP_DATA_DIR}/data/config.yaml" ]; then
  install -m 0666 -o 1000 -g 1000 "${APP_SRC_DIR}/config/bridge.yaml" "${APP_DATA_DIR}/data/config.yaml"
fi

chmod -R 777 "${APP_DATA_DIR}/data"
chmod -R 777 "${APP_DATA_DIR}/kaspad_data"

